#!/bin/bash

# omarchy:summary=Restart the hyprsunset service (used for blue light filtering/night light).
if pgrep -f "hyprsunset_daynight" > /dev/null; then
    systemctl --user restart hyprsunset-daynight
fi
